{
  "name": "nfr-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page for Neural Frequency Resonance",
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static site - no build needed'"
  }
}
